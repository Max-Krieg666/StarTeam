.page-header
  %h1 Стадионы команд

%table.table.table-bordered.table-hover.table-condensed
  %thead
    %tr
      %th.text-center
        .fa.fa-pencil
        Название
      %th.text-center
        .fa.fa-users
        Команда
      %th.text-center
        .fa.fa-male
        Вместительность
      %th.text-center
        .fa.fa-level-up
        Уровень
      %th
      -if @current_user.role!=0
        %th
        %th
      
  %tbody
    - @stadia.each do |stadium|
      %tr.text-center
        %td= stadium.title
        %td
          = link_to stadium.team.title, stadium.team, style: "color: #00F"
        %td= stadium.capacity
        %td= stadium.level
        -if @current_user.role!=0
          %td
            = link_to edit_stadium_path(stadium), class: "btn btn-link" do
              =fa_icon "edit"
          %td
            = link_to stadium, :method => :delete, :data => { :confirm => 'Вы уверены?' }, class: "btn btn-link" do
              =fa_icon "remove"
        %td
          = link_to stadium, class: "btn btn-link" do
            =fa_icon "arrow-circle-right"