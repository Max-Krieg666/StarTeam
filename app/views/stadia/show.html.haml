%p
  %b Владелец:
  = link_to @team.title, @team, style: "color: #00F"
%p
  %b Название:
  = @stadium.title
%p
  %b Вместительность:
  -c=@stadium.capacity
  -u=User.find(@team.user_id)
  -if @current_user==u || @current_user.administrator?
    = form_for @stadium do |f|
      .row
        .col-md-3
          .input-group
            =f.number_field :capacity, class: "form-control", min: c+1
            %span{class:"input-group-btn"}
              .actions
                = f.submit 'Увеличить', class: 'btn btn-default'
%p
  %b Уровень:
  = @stadium.level
  -if @stadium.level<5
    =link_to "+",{action: :edit,level: @stadium.level+1}, class: 'btn btn-default btn-sm'
.btn-group
  = link_to @stadium.team, class: 'btn btn-info' do
    =fa_icon "users"
    =@team.title
  -if @current_user==@user || @current_user.moderator?
    = link_to edit_stadium_path(@stadium), class: 'btn btn-danger' do
      =fa_icon "edit"
      Редактировать
    -#=link_to 'Расширить стадион', class: 'btn btn-success'
  -if @current_user.moderator?
    = link_to stadia_path, class: 'btn btn-warning' do
      =fa_icon "reply"
      Все стадионы
